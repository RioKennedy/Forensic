# Volatility3
- [NetScan](Volatility3測試/V3NetScan.md)

'''
| 類別   | Plugin                    | 功能說明                            |
| ---- | ------------------------- | ------------------------------- |
| 行程列表 | `windows.pslist.PsList`   | 列出正常鏈結中的 Process                |
| 行程掃描 | `windows.psscan.PsScan`   | 掃描記憶體中可能存在的 Process，可發現隱藏或已結束行程 |
| 行程樹  | `windows.pstree.PsTree`   | 以父子關係顯示 Process                 |
| 命令列  | `windows.cmdline.CmdLine` | 顯示 Process 啟動時的命令列參數            |
| 環境變數 | `windows.envars.Envars`   | 顯示 Process 的環境變數                |
'''
